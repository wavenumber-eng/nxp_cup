+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-26T173302-0600"
plan_id = "macos-core-tools-release"
step_id = "macos-programmer-integration"
created = "2026-08-26T17:33:02-06:00"
+++

# macOS pinned programmer and verified flash

The CMake-built arm64 `nxpc_tool` resolved the executable-relative pinned
`rblhost 0.2.0`, validated the 357,104-byte competition BIN and its vector table, and
selected exactly one attached runtime device. It completed the guarded runtime-to-ROM
transition, ROM identity query, erase-all, write at address zero, full 357,104-byte
readback, temporary-file cleanup, SHA-256 comparison, reset, and application reconnect.

The readback SHA-256 matched
`BE41ECC5085D71F804BFA3D61EB61A293C75805B899A137D8BAC340C63EC6811`.
Post-program verification completed a new AVCU session and received a complete camera
frame with zero malformed packets. The full operation took about nine seconds.

Subprocess output remains capped at 1 MiB and each query, erase, write, readback, and reset
stage retains an explicit timeout. The macOS temporary path, SHA-256, environment lookup,
and executable-relative programmer resolution passed on physical Apple Silicon hardware.
