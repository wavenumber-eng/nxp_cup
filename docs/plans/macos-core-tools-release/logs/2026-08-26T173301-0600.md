+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-26T173301-0600"
plan_id = "macos-core-tools-release"
step_id = "macos-device-io"
created = "2026-08-26T17:33:01-06:00"
+++

# macOS runtime and ROM device I/O

On the attached FRDM-MCXN947, the Mac host uniquely discovered runtime CDC
VID/PID `1FC9:0094` as `/dev/cu.usbmodem1101`, opened it with bounded reads and
writes, completed HELLO and channel negotiation, retained bounded telemetry and logs, and
closed the session with zero malformed packets. A camera-enabled probe received one
complete 320x200 RGB565 frame of 128,000 bytes.

Changed ROM HID discovery to enumerate metadata without opening every HID device on the
Mac. Opening the global HID manager unnecessarily triggered macOS protection for unrelated
input devices; read-only property enumeration needs no Input Monitoring permission. The
runtime ENTER_ISP request then exposed exactly one `1FC9:014F` ROM HID device, and the
pinned programmer reset it back to an application that reconnected successfully.

Selection continues to match the frozen VID/PID values and refuses zero or multiple
matching targets. No actuator command was sent; the observed application remained in
TEST/DISARMED with motors disabled.
