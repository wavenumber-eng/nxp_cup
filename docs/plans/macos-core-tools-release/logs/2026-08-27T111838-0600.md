+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-27T111838-0600"
plan_id = "macos-core-tools-release"
step_id = "macos-student-setup"
created = "2026-08-27T11:18:38-06:00"
+++

# Apple Silicon student workflow checkpoint

Added `docs/setup-macos.md` as the explicit Apple Silicon macOS 13-or-newer
student workflow and linked it from the repository and component entry points.
The guide preserves `setup.sh` as the only Mac shell bootstrap and invokes the
existing embedded and host PowerShell scripts with `pwsh -File`; it does not add
parallel build or flash wrappers.

The current preview instructions use `./setup.sh -SkipCoreTools`, verify the
organizer-provided ZIP and checksum, extract it under `out/artifacts/host`, and
run the packaged self-test. They then cover the normal competition build,
runtime device and frame probe, ROM-first flash, full recovery-button sequence,
viewer launch, and the optional maintainer host build and ad-hoc package command.
The guide clearly states the Apple Silicon limit and describes the bounded
macOS Privacy & Security `Open Anyway` path without disabling Gatekeeper.

The unsigned-distribution choice is represented accurately: the package has no
Developer ID identity and is not notarized, while its ad-hoc signature preserves
the app bundle's structural integrity. The setup step remains active because no
stable public Mac asset URL and immutable clean-source checksum are pinned yet.

The focused Windows and Mac setup documentation contracts pass (32 tests),
`git diff --check` passes, and the `docs.plans` audit passes with 153 logs.
