+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-26T154500-0600"
plan_id = "macos-core-tools-release"
step_id = "reproducible-rblhost-build"
created = "2026-08-26T15:45:00-06:00"
+++

# Pinned macOS arm64 rblhost build

Built NXP rblhost v0.2.0 from exact commit
`7a775dde2c44bd345a1ac067698afa999bd71be0` and the upstream lockfile with Rust/Cargo
1.96.1. Two clean source checkouts at different absolute paths produced the identical
arm64 Mach-O SHA-256
`d75babe663d783ad83a5c5b760ef0581080227031f8d74256fc282d3d2c76344`. The executable
reports `rblhost 0.2.0` and links only macOS system frameworks and libraries.

Added the verified binary as `src/host/vendor/rblhost/rblhost-macos-arm64`, alongside the
existing Windows payload. Added `build.versions.json` as the shared upstream commit, Rust
version, platform file-name, and SHA-256 contract. Updated the vendor record without
claiming Mac board validation.

Added `src/host/tools/build-rblhost.ps1`. On Apple Silicon it requires the exact Rust and
Cargo version and target host, optionally clones only into `out`, refuses a dirty or wrong
source commit, builds with `cargo build --locked --release`, checks the reported version
and pinned hash, and publishes only the verified result under `out/build/host/vendor`.
The verifier passed against the second clean checkout.
