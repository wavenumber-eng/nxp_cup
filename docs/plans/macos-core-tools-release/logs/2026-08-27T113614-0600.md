+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-27T113614-0600"
plan_id = "macos-core-tools-release"
step_id = "macos-board-acceptance"
created = "2026-08-27T11:36:14-06:00"
+++

# Clean-checkout Apple Silicon setup and build

Created local branch `macos-clean-setup` and cloned commit
`326ce5f7b723346ecca66dab3a3711cec6904594` with `--no-local` into
`/tmp/nxpc-clean-setup.3zpTLo`. The checkout began with no `out` directory and a
clean worktree.

Plain `./setup.sh`, with no skips and no archive borrowed from the original
workspace, downloaded the 134,812,148-byte official Arm GNU 14.2.Rel1 archive
into the new checkout, verified its pinned SHA-256, extracted approximately
13,000 files, and installed GCC 14.2.1 under that checkout's `out/toolchains`.
It accepted the installed CMake 4.3.0 and Ninja 1.13.2, then configured and built
all 273 native host/SDL steps from the repository's pinned inputs. The staged
Apple Silicon app passed strict deep ad-hoc signature validation, the local CLI
self-test passed, and setup installed the runtime under the new checkout's
`out/artifacts/host` without a Developer ID, notarization, Rust, or MCUXpresso.

A second plain `./setup.sh` run verified and skipped both the compiler and local
host runtime. The unchanged competition firmware then configured with the new
checkout's repository-local `arm-none-eabi-gcc`, built all 87 steps, verified 84
C compile commands, and reproduced the 357,104-byte BIN SHA-256
`be41ecc5085d71f804bfa3d61eb61a293c75805b899a137d8bac340c63ec6811`.

Focused setup documentation contracts pass 32 tests, and deterministic Mac
package contracts pass two tests with one opt-in release test skipped. The
`macos-student-setup` and `automated-macos-contract-tests` steps are complete.
`macos-board-acceptance` is active: its final clean-checkout probe/viewer/flash
attempt found neither runtime CDC nor ROM HID because the board was no longer
attached. Earlier board evidence remains valid, but the clean-checkout hardware
confirmation will be repeated when the car is reconnected. No firmware source
or complexity baseline was modified.
