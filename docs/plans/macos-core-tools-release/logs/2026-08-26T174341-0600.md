+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-26T174341-0600"
plan_id = "macos-core-tools-release"
step_id = "portable-build-entry-points"
created = "2026-08-26T17:43:41-06:00"
+++

# Portable component entry points

Installed native arm64 PowerShell 7.6.5 through Homebrew and exercised the existing
component entry points from the repository root. `src/host/build.ps1` selected AppleClang,
the Mac CMake tree and payload names, built the app and CLI, and published `NXP Cup
Viewer.app`, `nxpc_tool`, `rblhost`, and the programmer license under
`out/artifacts/host`. The same script retains its Windows Clang/MSVC branches and Windows
payload names.

`src/embedded/build.ps1 -Clean` configured and completed all 87 Ninja steps, verified 84
C compile commands against the competition flag contract, and republished the 357,104-byte
BIN. This workstation still used the installed MCUXpresso compiler fallback; repository-
local clean-Mac toolchain provisioning remains owned by `macos-student-setup`.

Updated `flash.ps1` to select the extensionless Mac CLI, platform temporary directory and
Mac J-Link Commander locations while retaining the Windows paths. The shared Arm-tool and
J-Link serial resolvers now accept extensionless Mac tools and read Mac USB metadata
without opening probes. With only J11 attached, the J-Link inventory correctly found zero
SEGGER probes.

The ordinary `src/embedded/flash.ps1 -Backend Rom` command completed runtime-to-ROM,
erase, write, full readback, matching SHA-256, reset, reconnect and a complete camera frame.
The full foundation suite passed: 85 tests passed and four environment-dependent tests
were skipped. Published CLI self-test and a two-second app hardware smoke test also passed.
