+++
type = "plan_log"
id = "macos-core-tools-release-2026-08-27T090127-0600"
plan_id = "macos-core-tools-release"
step_id = "macos-student-setup"
created = "2026-08-27T09:01:27-06:00"
+++

# Connected-board acceptance checkpoint

With the Rev A FRDM-MCXN947 connected to this Apple Silicon Mac, the packaged native
tools discovered the runtime CDC interface as `/dev/cu.usbmodem1201`, USB
VID/PID `1fc9:0094`, and completed the AVCU version-1 handshake. A three-second command
line probe received all four enabled channels, one complete 320 x 200 RGB565 camera
frame (128,000 bytes), four telemetry values, statistics, and a diagnostic log with zero
malformed packets or resynchronization bytes.

The packaged `NXP Cup Viewer.app` hardware test connected once and displayed 64 camera
frames in three seconds with zero malformed packets. The official
`src/embedded/flash.ps1 -Backend Rom` flow then requested ISP from the running
application, discovered ROM HID `1fc9:014f`, queried the bootloader, erased application
flash, wrote the locally built 357,104-byte competition image, performed a full readback,
and matched SHA-256
`be41ecc5085d71f804bfa3d61eb61a293c75805b899a137d8bac340c63ec6811`.
After reset, the board returned on the same runtime CDC interface and immediately passed
another complete camera/telemetry probe with no malformed data or device send errors.

This supplies connected-board evidence early while `macos-student-setup` remains active.
The dependent `macos-board-acceptance` step is not marked complete: its clean released
tool installation, physical ROM-entry/recovery exercise, and immutable signed/notarized
Mac release prerequisites remain open.
